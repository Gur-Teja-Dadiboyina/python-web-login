pipeline {
    agent any

    environment {
        // ---- change these to your values ----
        IMAGE          = "gurudadiboyina/python-login"   // Docker Hub repo: username/reponame
        AWS_REGION     = "ap-south-1"                    // Mumbai (fits your EC2 IP range)
        CLUSTER_NAME   = "my-eks-cluster"          // Your EKS cluster name
        K8S_DEPLOYMENT = "python-login-deployment"       // metadata.name in deployment.yaml
        K8S_SERVICE    = "python-login-service"          // metadata.name in service.yaml
        APP_PORT       = "8082"                          // container/service port
        // -------------------------------------
    }

    stages {
        stage('Checkout') {
            steps {
                // Option A: hardcode your repo like in the reference:
                 git branch: 'main', url: 'https://github.com/Gur-Teja-Dadiboyina/python-web-login.git'
                // Option B: use the job's SCM settings:
                checkout scm
            }
        }

        stage('Set Dynamic Tag') {
            steps {
                script {
                    def timestamp = new Date().format('yyyyMMdd-HHmm', TimeZone.getTimeZone('UTC'))
                    env.TAG = "v${env.BUILD_ID}-${timestamp}"
                    echo "Using image tag: ${env.TAG}"
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                sh """
                  docker build \\
                    --build-arg PORT=${APP_PORT} \\
                    -t ${IMAGE}:${TAG} .
                """
            }
        }

stage('Push Docker Image') {
    steps {
        withCredentials([usernamePassword(credentialsId: 'dockerhub-credentials', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
            sh '''
                echo "$DOCKER_PASS" | docker login -u "$DOCKER_USER" --password-stdin
                docker push '"${IMAGE}:${TAG}"'
                docker logout || true
            '''
        }
    }
}

        stage('Update Deployment YAML') {
            steps {
                // Assumes deployment.yaml has a single "image:" line to replace
                sh "sed -i 's|image: *.*|image: ${IMAGE}:${TAG}|' deployment.yaml"
            }
        }

        stage('Configure AWS & Kubeconfig') {
            steps {
                withCredentials([[ $class: 'AmazonWebServicesCredentialsBinding', credentialsId: 'aws-eks-creds' ]]) {
                    sh '''
                        aws eks update-kubeconfig --name '"$CLUSTER_NAME"' --region '"$AWS_REGION"'
                    '''
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                withCredentials([[ $class: 'AmazonWebServicesCredentialsBinding', credentialsId: 'aws-eks-creds' ]]) {
                    sh """
                        # Apply manifests
                        kubectl apply -f deployment.yaml
                        kubectl apply -f service.yaml

                        # (Optional) ensure correct image if your YAML isn't templated
                        kubectl set image deployment/${K8S_DEPLOYMENT} \\
                          ${K8S_DEPLOYMENT}=\\
${IMAGE}:${TAG} --record || true

                        # Rollout status
                        kubectl rollout status deployment/${K8S_DEPLOYMENT} --timeout=180s
                    """
                }
            }
        }

        stage('Print Public URL') {
            steps {
                withCredentials([[ $class: 'AmazonWebServicesCredentialsBinding', credentialsId: 'aws-eks-creds' ]]) {
                    sh """
                        # For a LoadBalancer-type service
                        PUBLIC_HOST=\$(kubectl get svc ${K8S_SERVICE} -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')
                        if [ -z "\$PUBLIC_HOST" ]; then
                          # Some clusters use 'ip' instead of 'hostname'
                          PUBLIC_HOST=\$(kubectl get svc ${K8S_SERVICE} -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                        fi
                        echo "Your application is live at: http://\${PUBLIC_HOST}:${APP_PORT}/"
                    """
                }
            }
        }
    }
}
